@*@model UserModel*@
@{
    ViewData["Title"] = "Home Page";
}

<script src="~/lib/jquery/dist/jquery.js"></script>
@*@if (Model != null)
{

        <p>@Model.token</p>
}
            else
            {
                <p>rỗng</p>
            }
<p>viewbag:@ViewBag.token</p>*@
<form id="formLogin" method="post" asp-action="Index" asp-controller="Home">
    <label>Email</label>
    <input id="mail" type="text" name="Email" class="form-control" />
    <label>Password</label>
    <input id="pass" type="password" name="Password" class="form-control" />
    <label class="text-danger col-form-label">@ViewBag.status</label>
    <input id="btnLogin" type="submit" class="mt-3 btn btn-success form-control" value="Login" />
</form>
<p id="value"></p>

@*login and get token with jquery*@
@*<script>
    $(document).ready(function () {

        $("#btnLogin").click(function (e) {
            const obj = {
                Email: $('#mail').val(),
                Password: $('#pass').val()
            }
            localStorage.clear()
            //$.post("https://localhost:44306/api/Token", JSON.stringify(obj), function (data) {
            //    console.log(data);
            //})
            $.ajax({
                //headers: {
                //    'Accept': 'application/json',
                //    'Content-Type': 'application/json'
                //},
                contentType: 'application/json',
                url: "https://localhost:44306/api/Token",
                type: 'POST',
                data: JSON.stringify(obj),
                success: function (data) {
                    localStorage.setItem('Token', data);
                    console.log(localStorage.getItem('Token'), 'day la token');
                }
            })
        });
    });
</script>*@